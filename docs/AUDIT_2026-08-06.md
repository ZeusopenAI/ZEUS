# Audit repository Quang Quý AI — 2026-08-06

## Phạm vi

Repository `/data/data/com.termux/files/home/quangquy-ai`, code vendored tại `agents/hermes`, local runtime Termux và hai remote GitHub công khai.

## Thống kê

- 6,132 tracked files; 6,122 nằm dưới `agents/`.
- Thành phần lớn nhất theo số file: 2,902 Python, 1,394 Markdown, 777 TypeScript, 418 TSX, 114 YAML.
- Số dòng thô theo định dạng: Python 1,357,719; Markdown 438,895; TypeScript 148,320; TSX 113,698. Đây là line count, không phải LOC đã loại comment/generated code.
- Công cụ `pygount` không có trong môi trường; audit dùng thống kê tracked files trực tiếp.

## Kết quả kỹ thuật

### Tốt

- Git working tree sạch trước audit; branch `main` theo dõi `origin/main`.
- Không có submodule; Hermes là vendored snapshot có source marker.
- Source marker và remote Hermes `main` cùng SHA `5e51b123f32b7f6a51fbd5759e89ba5146ce4003`.
- `python -m compileall -q agents/hermes` exit 0.
- `python -m pip check` trả `No broken requirements found`.
- High-confidence secret scan trên production paths hiện tại trả 0 finding.
- Không có sensitive filename được track ngoài `.env.example`; lịch sử filename scan cũng chỉ thấy `.env.example`.

### Critical/High

1. `npm audit` trả 12 advisories: 1 critical, 10 high, 1 low. Critical liên quan `tar`; high gồm `brace-expansion`, `concurrently`, `electron`, `fast-uri`, `js-yaml`, `postcss`, `react-router`, `react-router-dom`, `shell-quote`, `undici`.
2. Android không có package `com.termux.boot`; cấu hình boot đúng nhưng chưa thể chạy sau reboot thật.
3. Full Python test chưa được xác nhận trên Termux vì test runner gặp `PermissionError` ở subprocess isolation. Cần chạy trên CI Linux/VPS.
4. Root workflow không chạy full CI của Hermes. Workflow vendored tại `agents/hermes/.github/workflows/ci.yml` là inert trên repository cha; hiện chưa có pytest, lint/typecheck, frontend build, lockfile check hoặc OSV scan bắt buộc trước merge.

### Medium

1. GitHub Actions trước audit dùng mutable tag `actions/checkout@v4`; đã đổi sang commit SHA pin trong working tree.
2. README trạng thái còn nói repository mới khởi tạo và chưa nhập mã nguồn, không còn đúng sau khi Hermes đã được vendored.
3. Workflow validation chỉ chạy compile/metadata/secret checks, chưa chạy test suite và Node builds.
4. `gh` chưa authenticated nên branch protection, secrets inventory và private settings chưa được xác minh.
5. Ba icon installer tên `32x32.png`, `128x128.png` và `128x128@2x.png` đều là cùng PNG 1024×1024, 689.840 byte; cần tạo đúng kích thước trước khi đóng gói Tauri.
6. `agents/hermes/apps/bootstrap-installer/src-tauri/Cargo.toml` dùng dependency range rộng và không có `Cargo.lock`, làm giảm tính tái lập/supply-chain pinning của installer.

### Low/Information

- `compileall` phát một `SyntaxWarning` tại `agents/hermes/tests/agent/test_credits_tracker.py:625` do escape `\d` trong docstring test.
- Có 15 nhóm file trùng hash. Phần lớn có vẻ chủ ý: font dùng cho desktop/web, template hội nghị, ảnh branding, package `__init__.py`. Nhóm icon bootstrap có ba tên kích thước nhưng cùng nội dung 689,840 byte, nên kiểm tra asset pipeline khi chỉnh installer.
- Node `node_modules` không tồn tại; audit dựa trên lockfile, chưa build frontend tại local.

## Khuyến nghị cấu trúc

- Dùng `quangquy-ai` làm product/control-plane canonical; dùng `qquy28888-ops/hermes-agent` làm technical fork của `NousResearch/hermes-agent`.
- Thay snapshot copy bằng Git submodule/version pin. Nếu bắt buộc monorepo, dùng subtree không `--squash` để giữ ancestry.
- Đặt tùy biến Quang Quý trong config, skills/plugins riêng hoặc thư mục integration; tránh sửa core Hermes nếu không cần.
- Thực hiện migration trên branch có backup tag, tree-equivalence check và rollback; không rewrite `main` trực tiếp.
- Đồng bộ fork đang chậm 6.045 commit với upstream theo từng batch có review/test; không kéo thẳng lên production.

## Ưu tiên xử lý

1. Cài Termux:Boot và reboot test.
2. Remediate npm critical/high trên nhánh riêng.
3. Bổ sung CI full test/build và branch protection.
4. Cấu hình Telegram E2E.
5. Chuyển production runtime sang VPS.
